/** Must match backend guest upload validation (OpenAPI description). */
export const GUEST_PHOTO_CONSENT_TEXT =
  'I have permission to share this photo and understand it may appear on the event gallery.'

/** Must be acknowledged when hosts enable Photo Share (OpenAPI description). */
export const HOST_PHOTO_GALLERY_ACK_TEXT =
  'Approved photos are visible to anyone with this event link.'

export const PUBLIC_PHOTOS_PAGE_SIZE = 50
